name: fameverse_live
description: Fameverse native client migration with Supabase authority and Stream Video media.
publish_to: none
version: 0.2.0+1

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  camera: 0.12.0+2
  cupertino_icons: ^1.0.8
  image_picker: 1.2.3
  stream_video_flutter: 1.6.0
  supabase_flutter: ^2.17.2
  url_launcher: ^6.3.2
  video_player: 2.11.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
